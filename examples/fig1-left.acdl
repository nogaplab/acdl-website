ReactNoReasoningInHistory[@T]: {
  S: {
    INSTRUCTIONS
    AVAILABLE_TOOLS
  }
  U: env.user_input[@1]
  // history
  ForEach(t: range(1,@T)) {
    A: {
      sys.tool_used[@t]
    }
    T: sys.tool_used[@t].tool_response
  }
  S: USE_TOOLS_TO_SOLVE_TASK 
}